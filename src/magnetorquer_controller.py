

class MagnetorquerController:
    def detumble(self, adcs_data):
        adcs_data_frame = self._dbus_server.get_adcs_data()

        # Do stuff

        magnetorquer_command = (0, 0)
        return magnetorquer_command

    def point(self, adcs_data):
        adcs_data_frame = self._dbus_server.get_adcs_data()

        # Do stuff

        magnetorquer_command = (0, 0)
        return magnetorquer_command
